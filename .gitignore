# Live-build artifacts
.build/
binary/
cache/
chroot/
/local/

# Output
*.iso
*.iso.zsync.xz
*.img
*.log

# lb build generated manifests
binary.contents
binary.packages
chroot.headers
chroot.packages.install
chroot.packages.live

# lb config generated files (regenerated by lb config)
config/binary
config/bootstrap
config/chroot
config/common
config/source

# GPG key binaria (se descarga en build time)
config/archives/nodesource.key.chroot
